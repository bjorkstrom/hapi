TODO:

for POST /models/new
 check the file extension, and do some error reply (400?) on unsupported extensions

for DELETE /models/{modelId}

 don't allow to delete models that are instanciated on devices

rename hagring-api.yaml -> hagring.yaml

rename host/ to server/

